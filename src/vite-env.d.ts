    /// <reference types="vite/client" />

type scrollNode = {
    image: string,
    title: string,
}

type contentNode = {
    icon: React.ReactNode,
    title: string,
    description: string,
    buttonText: string,
}